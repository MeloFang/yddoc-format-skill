---
name: company-doc-format
description: 按本公司格式规范起草、改写、压缩和复核各类文档，包括公文（通知、报告、批复、请示）以及汇报材料、发言稿、普通文章、方案等。触发场景：用户说「写一份 / 起草 / 出一份通知（报告、请示、批复、汇报材料、发言稿、方案）」「按公司要求 / 按公司规范调整」「改成公司格式」「套公司模板」「公司格式校对」「调一下排版」，或提交任何需要统一为公司版式的文稿、Word 文件。凡是涉及本公司公文与对内材料的起草或改稿，一律默认套用本规范，无需用户明说「按公司格式」。公司版式参数（页面、字体、行距、层级序数、落款）对公文和普通文章一视同仁；材料中的表格用微软雅黑、图表居中且不跨页；每次改稿同步检查错别字并优化遣词造句。通用文种规则与行文关系由 chinese-official-writing 技能负责；两者同时适用时以公司规定为准。
---

# 公司文档格式工作流

> **开源版说明**：本规范源自一家真实企业的公文与材料实践，参数（页边距、字体、行距、文种口径等）
> 已按通用中文公文习惯整理，**可直接用，也可按你所在单位的规定修改**。
> 需要改动时优先改 references/format-rules.md（版式参数）与 references/genre.md（文种口径）。
> 具象信息（单位名、文号代字、金额等）已全部脱敏为占位符，不含任何企业敏感内容。



## 零、一句话触发后怎么走（新对话直接开工）

用户通常只说一句「写份通知」「出个 8 月降本增效的汇报材料」，不会重复交代格式要求。收到这类需求时：

1. **默认套用本规范**，不要反问"要不要按公司格式"——凡涉及本公司公文与对内材料，公司规范就是默认项。
2. **先判断缺什么**，一次性把要素问齐（清单见第 1 步），不要挤牙膏式反复追问。
3. **要素够了就先给正文**，直接在对话里出成稿，**不主动生成文件**；用户补一句"出个文件""给我 Word"再走第 7 步。
4. **禁止编造**：单位名称、文号代字、联系人电话、金额标准、成文日期、签发人一律向用户索取，或用占位符（`<发文单位>`、`<代字>〔YYYY〕N号`）。
5. 成稿后**必做第 6 步校对**，并按"必改 / 建议"两级附改动清单。

### 应命中本技能的说法（示例）

"写份关于XX的通知" / "起草一个请示" / "出份汇报材料" / "把这个改成公司格式" / "压缩到 300 字" / "校对一下这份稿子" / "套公司模板出个 Word" / "按公司规范调一下排版"。

## 适用范围（动手前先判断是哪一类）

本技能覆盖两类对象，**版式基底完全相同**——页面、字体、行距、层级序数、落款规则对两者一视同仁。

| 要素 | 公文 | 普通文章 / 材料 |
| --- | --- | --- |
| 标题 | 发文单位 + 关于 + 某事 + 文种，方正小标宋简体三号居中 | 按文章标题，同样小标宋三号居中 |
| 主送机关 | 有（如"各分公司："） | 无 |
| 结尾语 | 按文种固定 | 无 |
| 发文字号 | 版头与文件名体现 | 无 |
| 红头 | **系统自动生成，本技能不处理** | 无 |
| 表格 / 图片 | 基本不出现 | **常见**：表内微软雅黑，图表居中，小表小图不跨页 |
| 页面 / 字体 / 行距 / 层级 / 落款 | 同一套规范 | **同一套规范** |

**汇报材料 / 发言稿**特有的三件事（详见 `format-rules.md` 第九节）：表格、图片、语言润色。

### 两条重要的简化

- **红头一律不处理**：公司系统自动生成红头，本技能既不生成、也不校验红头，更不要向用户索取红头模板。
- **请示不特殊化**：请示与普通公文版式完全一致，差别只有三处——红头不同（系统生成）、结尾语用"以上妥否，请批示。"、需显示签发人并在文末加联系人。除此之外按普通公文走，不要额外叠加规则。

## 与其他技能的分工

- 文种判定、行文关系、论证链条、国标本位的版式，**由 `chinese-official-writing` 技能负责**，命中对应场景先加载它。
- 本技能只承载**公司特有要求**：版式参数、发文字号规则、内部称谓口径、禁用词、惯用套话、审批与用印流程。
- 通用规则与公司规定冲突时，**以公司规定为准**。
- 三层加载纪律：`SKILL.md` 常驻；`references/` 按需读；`assets/` 里的模板只复制使用，不读入上下文。

## 工作流（每次按顺序执行）

### 第 0 步：判定任务模式与文档类型

先分清是**起草、改写、压缩、还是只审不改**，以及是**公文还是普通文章**。

- 只审不改 → 输出问题位置、风险层级和修改建议，**不重写全文**，除非用户明确要求代改。
- 改写/压缩 → 输出改后正文。
- 起草 → 先核对要素，再按骨架成稿。

### 第 1 步：收集要素

**起草通知的最小要素集**（按此顺序核对）：

1. 发文单位全称（上行文必带，党委行文须带"党委"二字）
2. 主送范围（下行常为"各分公司："）
3. 事项——要解决什么问题、部署什么工作
4. 依据——上级文件 / 会议 / 制度，**须带文号**
5. 时限——何时起执行、何时前报送
6. 责任部门 / 落实主体
7. 有无附件
8. 是否需落款与成文日期

**请示**额外要：签发人、联系人及电话、请批事项（一文一事）。
**普通文章**要：主题、用途、受众、篇幅、是否需落款。

提问纪律：

- 只有**影响成稿正确性**的才必问：发文单位、事项、依据、时限。
- 可以合理默认的（主送用"各分公司："、普通通知不带落款）**直接默认，成稿后一句话说明**，不要为琐事打断用户。
- 缺失项一律列为待补充，**不得编造**；电话、邮箱、日期、文号、签发人、印章、审批结论尤其禁止虚构。

### 第 2 步：判定文种（仅公文需要）

读 `references/genre.md`，确认文种、主送写法、结尾语。普通文章跳过此步。

### 第 3 步：校准版式

读 `references/format-rules.md`，逐项核对页面设置、字体字号、行距、标题层级、附件说明、落款与日期。

### 第 4 步：处理表格与图片（材料类文档必做）

读 `references/format-rules.md` 第九节，逐项处理：

1. **表格**：表内文字**微软雅黑**（表头加粗）、**居中**；内容少的按内容收缩后再居中；表题在表上方居中。
2. **图片**：**居中**，按比例缩放至不超版心宽度 15.6 cm；图题在图下方居中。
3. **不跨页**：小表小图整体排在一页内，临近页尾就下移或微调；**一页放不下的大表才跨页，且必须重复标题行**。
4. **允许突破**：为给图表让位，可突破"禁止空行、行距 1.5 倍、每页 22 行、字号小四"四项——图表与正文之间空一行、表内用较小行距与字号，都是合理的。
5. 不可突破：页面设置、页眉页脚、正文本身的字号行距、标题、落款。

### 第 5 步：校订语言与口径

读 `references/wording-rules.md`，替换禁用词、统一内部称谓、去除口语化和 AI 模板腔。

### 第 6 步：校对错别字与优化表达（每次必做）

读 `references/review-rules.md`，逐段过：

- **错别字 / 易混词**（部署≠布署、截至≠截止、账号≠帐号、的得地等）→ **必改**。
- **病句**（缺主语、搭配不当、超长句）→ **必改**。
- **冗余、套话、AI 腔** → 建议优化。
- **不改**：事实、数据、人名、机构名、专业术语、用户刻意保留的表达。
- 输出时按"必改 / 建议"两级列**改动清单**（位置、原文、改为、理由）；用户要"直接改好"就正文用改后版本，清单附在末尾。

### 第 7 步：交付

> **铁律：用户的原文件一个字节都不许改。** 所有调整一律**另存新文件**，让用户能打开新旧两份对照。

**文件处理准则**

1. 用户给的是**已存在的文件**（docx / md / txt）→ 原文件**保持不动**，复制一份再改。
2. 新文件命名：`<原文件名>（公司格式调整）.docx`；同批次第二版用 `<原文件名>（公司格式调整v2）.docx`，或使用日期后缀 `<原文件名>（调整-20260908）.docx`。
3. 新文件放**与原件同一目录**，方便并排对比；不要挪动或重命名原件。
4. 交付时**同时报出两个路径**：原件（未改动）与调整后文件各是什么。
5. 只有在用户**明确说"就改这个文件 / 覆盖"**时，才可以直接写回原件——即便如此，也先确认一次。

- **默认给正文**：用户只要内容 → 直接出成稿，不附说明、不生成文件。
- **用户补一句"出个文件 / 给我 Word"** → 再生成 docx，两条路：

**路线 A（推荐，参数已固化）**：用脚本从 Markdown 渲染

```bash
PY="python"   # 或用你的虚拟环境 python
SKILL="~/.workbuddy/skills/company-doc-format"
"$PY" "$SKILL/scripts/render_docx.py" draft.md out.docx
```

Markdown 约定：`# 标题`、`## 一级标题（黑体）`、`### 二级标题（楷体）`、普通行即正文、`{{SIGN}}` 落款单位、`{{DATE}}` 日期、`{{ATTACH:文件名}}` 附件。脚本已内置 A4、页边距上 3.7/下 3.5/左 2.8/右 2.6 cm、奇偶页不同、页眉 1.5/页脚 2.6 cm、仿宋_GB2312 小四、**行距 1.5 倍（2026-09-09 用户修正，原固定值 28 磅作废）**、落款下空 3 行 + 日期缩进 4 字符。

**路线 B（需要模板原有页眉页脚时）**：复制 `assets/发文模板-通知.docx` 到当前工作空间，再逐段写入正文，**不要改动 assets 里的原件**。

- 两种路线都**另存新版本，不覆盖原件**（见上方文件处理准则）；生成后调用 `present_files` 打开预览。
- **汇报材料含表格 / 图片时**：模板与脚本都不含图表样式，需另行设置——表格微软雅黑五号、表头加粗居中、数字右对齐、页面居中、宽不超 15.6 cm；图片居中缩放。详见 `format-rules.md` 第九节。
- 交付前跑一遍下方复核清单。

## 硬边界（公司明文规定，优先于一切通用规则）

1. **正文：中文仿宋_GB2312、西文 Times New Roman、小四；行距 1.5 倍（2026-09-09 用户修正，原固定值 28 磅作废）；一律不加粗。**
2. **正文中不得出现空格和空行**；结尾语与正文之间不留空行；**附件说明与正文之间空一行**（唯一例外）。
3. **层次序数为"一、"→"（一）"→"1."→"（1）"，不得套用自动编号**。字体按**性质**判：一级黑体、二级楷体、三四级仿宋_GB2312——**仅当该序数段是标题**（短语式，如"（一）追责触发条件"）。**若序数引出的是完整叙述句/机制说明等正文内容**（如"（一）以上扣罚标准适用于……"），该段属正文，**用仿宋_GB2312**，不得因带序数就套楷体。
4. **结尾语按文种固定**（仅公文）：通知"特此通知。"、报告"特此报告。"、批复"特此批复。"、请示"以上妥否，请批示。"
5. **标题 = 发文单位 + 关于 + 某事 + 文种**（仅公文）；下行文可省发文单位，**上行文不可省**；统一用"的"+文种（两字）。
6. **标题用方正小标宋简体三号，居中**；页面 A4，**页边距上 3.7 / 下 3.5 / 左 2.8 / 右 2.6 cm**，**版式勾选「奇偶页不同」**，**页眉 1.5 cm、页脚 2.6 cm**，**每行 28 字、每页 22 行**。
7. **发文字号格式为"代字 + 〔年份〕+ 序号 + 号"**（抽象形式：`<代字>〔YYYY〕N号`），年份用六角括号、序号不编虚位。**具体代字一律向用户索取或让其从原文件复制，不得猜测、不得编造。**
8. **落款：正文下空 3 行，日期文本后缩进 4 字符**。用户未要求时不主动补落款；要求时须先确认发文机关全称与成文日期。
9. **年份写全四位**，不得简写（"18年"违规）；**引用文件必须附文号**，六角括号年份、从原件复制。
10. **请示显示签发人、文末加联系人**；**联合发文必须老总签发**；部门通知不发省公司。
11. **红头由系统生成，本技能不生成、不校验、不索取红头模板。**
12. 不得为显得完整而补写材料里没有的事实、数据、责任部门、流程或整改动作。
13. **知识库脱敏纪律**：单位名称、文号代字、具体业务名称、金额标准、联系人等具象信息**一律不入库**，需要时向用户索取或使用占位符（如 `<发文单位>`、`<代字>〔YYYY〕N号`、`XX分公司`）。本技能只保存**格式规则与写法结构**，不保存任何一份真实文件的实质内容。
14. **表格：表内文字微软雅黑**（表头加粗）、字号五号、表格**页面居中**、宽度不超版心 15.6 cm；表题在表上方居中。
15. **图片：居中**，按比例缩放至不超版心宽度；**小表小图不跨页**，一页放不下的**大表才跨页且必须重复表头**。为图表让位可突破空行、行距、网格、表内字号四项限制，**页面设置与正文本身的字号行距不可突破**。
16. **每次改稿必做校对**：检查错别字与易混词、修正病句、优化冗余表达，按"必改 / 建议"两级给出改动清单；**事实、数据、人名、机构名、专业术语一律不改**。
17. **绝不覆盖用户的原文件**：调整一律另存新文件（形如 `<原名>（公司格式调整）.docx`），原文件保持原样供对照；新文件与原件同目录。仅当用户明确要求"覆盖/就改原文件"时才写回，且先确认一次。这条同样适用于 `assets/` 下的模板——需要改模板时另存，不直接改写技能资产。
- 用户明确提出的层级动作（增/删自然段、调整顺序、替换标题、保留小标题）必须逐条落实，不得用近义词敷衍过去。
- 用户未要求落款、主送或成文日期时，不为补齐格式添加"上级单位""申请单位"之类泛称。

## 复核清单（交付前逐项过）

**公司硬性项**

1. 正文中文仿宋_GB2312、**西文 Times New Roman**、小四；**一级标题黑体、二级标题楷体_GB2312**、三级及以下仿宋_GB2312。**带序数但内容为完整叙述句的段落按正文处理（仿宋_GB2312），不算标题。**
2. **标题方正小标宋简体三号、居中**；页面 A4，页边距上 3.7 / 下 3.5 / 左 2.8 / 右 2.6 cm。
3. **版式勾选「奇偶页不同」；页眉 1.5 cm、页脚 2.6 cm；每行 28 字、每页 22 行。**
4. 行距 1.5 倍（原固定值 28 磅 2026-09-09 作废）；每个自然段首行缩进 2 字符。
5. 全文无加粗、无多余空格、无空行（**附件说明与正文之间空一行是唯一例外**；结尾语之前仍不空行）。
6. 层次序数为"一、""（一）""1.""（1）"，且非 Word 自动编号。
7. 结尾语与文种一一对应，且与正文之间无空行（仅公文）。
8. 标题符合"发文单位 + 关于 + 某事 + 文种"；上行文未省略发文单位（仅公文）。
9. 发文字号格式为"代字 + 〔年份〕+ 序号 + 号"，年份六角括号、序号不编虚位（仅公文）。
10. **落款：正文下空 3 行，日期后缩进 4 字符**（如需落款）。

**通用质量项**

11. 文种与行文方向正确，报告未夹带请示事项（公文）。
12. 称谓、机构名用全称且与公司口径一致（党委行文须带"党委"二字）。
13. 无禁用词残留、无口语化、无 AI 模板腔和项目卡片式套话。
14. 请批事项、联系人、附件、反馈渠道落位，无占位符残留。
15. 需要 Word 时，版式落在 `assets/发文模板-通知.docx` 上。
16. 附件排版正确：附件区与正文之间空一行；多附件序号列举、后续行对齐。
17. 请示已显示签发人、文末附联系人；引用文件带文号且用六角括号；年份四位全称。

**表格与图片项（材料类）**

18. 表内文字**微软雅黑**，表头加粗、居中，字号五号；数字右对齐。
19. 表格、图片**页面居中**；宽度不超版心 15.6 cm；表题在表上方、图题在图下方，均居中。
20. **小表小图未跨页**；跨页的大表已**重复标题行**。
21. 图表与正文之间空一行（允许的例外）；表格内行距按需要可另设（紧凑排布），**正文行距一律 1.5 倍**。

**文字校对项（必做）**

22. 无错别字与易混词误用（部署/布署、截至/截止、账号/帐号、的得地等）。
23. 无病句（缺主语、搭配不当、超长句已拆分）；并列结构对称。
24. 冗余、口语、套话、AI 腔已清理；评价性词有事实或数据支撑。
25. 改动清单已给出（必改 / 建议两级），且未改动事实、数据、人名与机构名。
26. **原文件未被改动**，调整结果已另存为新文件，且交付时同时给出原件与新文件路径。

## 参考资料

| 文件 | 什么时候读 |
| --- | --- |
| `references/genre.md` | 第 2 步（仅公文）：判定文种、行文方向、主送与结尾语 |
| `references/format-rules.md` | 第 3、4 步：页面字体行距层级、附件落款、**第九节表格与图片** |
| `references/wording-rules.md` | 第 5 步：统一用词、称谓、引用格式、替换禁用词 |
| `references/review-rules.md` | 第 6 步：错别字校对、病句修正、遣词造句优化 |

## 资产

- `assets/发文模板-通知.docx` — **脱敏版式模板**（沿用公司真实发文的版式，正文已替换为中性示意内容），已内置版式：A4、页边距上 37 / 下 35 / 左 28 / 右 26 mm，标题方正小标宋简体三号，正文仿宋_GB2312 小四，**行距 1.5 倍**（2026-09-09 已按用户要求从固定值 28 磅改为 1.5 倍）。
- 要求输出 Word 时，**复制该模板再写入正文**，不要从零生成版式。
- 该模板**已按规范修正**（2026-09-08）：一级标题设为黑体、二级标题（短语式）设为楷体_GB2312，去主送前的全角空格与标题末尾多余句号；第六部分"分点叙述"的四段按正文用仿宋_GB2312。
- **材料类文档注意**：该模板只有公文正文段落，不含表格与图片样式。给汇报材料排版时，表格与图片按 `format-rules.md` 第九节另行设置（微软雅黑 + 居中），不要照搬正文样式。
- 模板文件不进上下文，仅在排版环节复制使用。
- 注：模板实测页脚距离为 25 mm（2.5 cm），与用户给定的 2.6 cm 相差 1 mm；如严格按规范，需另存调整。
